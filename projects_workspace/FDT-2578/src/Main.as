package {
	import net.bigpoint.as3toolbox.localstorage.LocalStorage;

	import flash.display.Sprite;

	public class Main extends Sprite {
		/**
		 * 
		 * bla bla blub <codeph>SharedObject</codeph> dfuh fsad f.
		 * <p>
		 * <b>Warning:</b> When <codeph>failMode</codeph> bla.
		 * </p>
		 */
		
		public function Main() {
			// Launch your application by right clicking within this class and select: Debug As > FDT SWF Application
			LocalStorage;
		}
	}
}
